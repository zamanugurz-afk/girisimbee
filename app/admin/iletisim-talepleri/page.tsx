import { AdminContactRequestsView } from '@/features/admin/panel/components/AdminContactRequestsView';

export const metadata = {
  title: 'İletişim Talepleri — Admin — Girisimbee',
};

export default function AdminContactRequestsPage() {
  return <AdminContactRequestsView />;
}
