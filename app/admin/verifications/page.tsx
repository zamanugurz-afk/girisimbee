import { AdminVerificationCenterView } from '@/features/admin/panel/views/AdminVerificationCenterView';

export const metadata = { title: 'Doğrulama Merkezi — Yönetim' };

export default function AdminVerificationsPage() {
  return <AdminVerificationCenterView />;
}
