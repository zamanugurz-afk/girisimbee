import { AdminSettingsView } from '@/features/admin/panel/views/AdminSettingsView';

export const metadata = { title: 'Ayarlar — Yönetim' };

export default function AdminSettingsPage() {
  return <AdminSettingsView />;
}
