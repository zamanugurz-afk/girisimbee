/**
 * Hero orbit theme switch.
 * - `'galaxy'` → saklanan ilk konsept: roket / galaksi orbit (aktif)
 * - `'bee'` → arı orbit (yedek)
 */
export type HeroOrbitTheme = 'galaxy' | 'bee';

export const HERO_ORBIT_THEME: HeroOrbitTheme = 'galaxy';
